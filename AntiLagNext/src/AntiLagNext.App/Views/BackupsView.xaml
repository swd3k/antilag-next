<UserControl x:Class="AntiLagNext.App.Views.BackupsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
      <Button Content="REFRESH" Style="{StaticResource GhostButton}" Command="{Binding RefreshCommand}" Margin="0,0,8,0" />
      <Button Content="RESTORE" Style="{StaticResource PrimaryButton}" Command="{Binding RestoreSelectedCommand}" Margin="0,0,8,0" />
      <Button Content="DELETE" Style="{StaticResource DangerOutlineButton}" Command="{Binding DeleteSelectedCommand}" Margin="0,0,8,0" />
      <Button Content="OPEN FOLDER" Style="{StaticResource GhostButton}" Command="{Binding OpenFolderCommand}" />
    </StackPanel>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="1.3*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Border Grid.Column="0" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource BorderBrushSubtle}"
              BorderThickness="1" CornerRadius="20" Margin="0,0,16,0">
        <ListBox ItemsSource="{Binding Backups}" SelectedItem="{Binding SelectedBackup}"
                 Style="{StaticResource ModernListBox}" ItemContainerStyle="{StaticResource ModernListBoxItem}">
          <ListBox.ItemTemplate>
            <DataTemplate>
              <StackPanel>
                <TextBlock Text="{Binding OperationName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                <TextBlock Text="{Binding DisplaySummary}" FontSize="11" FontFamily="Consolas"
                           Foreground="{StaticResource MutedText}" Margin="0,4,0,0" TextWrapping="Wrap" />
              </StackPanel>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
      </Border>

      <Border Grid.Column="1" Style="{StaticResource GlassCard}" CornerRadius="20">
        <DockPanel>
          <TextBlock DockPanel.Dock="Top" Text="SNAPSHOT DETAIL" FontSize="10" FontWeight="Bold"
                     Foreground="{StaticResource MutedText}" Margin="0,0,0,12" />
          <ScrollViewer>
            <TextBlock Text="{Binding DetailText}" TextWrapping="Wrap" FontFamily="Cascadia Mono, Consolas"
                       FontSize="12" Foreground="{StaticResource MutedText}" LineHeight="18" />
          </ScrollViewer>
        </DockPanel>
      </Border>
    </Grid>
  </Grid>
</UserControl>
